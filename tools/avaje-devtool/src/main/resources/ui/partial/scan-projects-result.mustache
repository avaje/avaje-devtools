<p>
  Found {{ projects.size }} top level projects in {{ directoriesSearched }} directories scanned.
  With {{ allCount }} projects in total including subprojects.
</p>
<p class="my-4">
  <button class="btn btn-secondary"
          hx-post="/addScannedProjects"
          hx-target="#content"
          hx-swap="innerHTML">
    add all {{ allCount }} scanned projects
  </button>
</p>
<p id="add-scanned-projects-result"></p>

<table class="table">
<tbody>
{{#projects}}
  <tr>
    <td>{{ groupId }}</td>
    <td>{{ artifactId }}</td>
  </tr>
{{/projects}}
</tbody>
</table>
